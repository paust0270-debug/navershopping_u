# ============================================================
# Turafic Slot-Naver Runner 환경변수
# ============================================================

# ============ 필수 설정 ============

# Production DB (slot_naver 테이블)
SUPABASE_PRODUCTION_URL=https://your-project-id.supabase.co
SUPABASE_PRODUCTION_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============ Heartbeat/모니터링 (선택) ============

# Control DB (워커 모니터링용 - 없으면 heartbeat 비활성화)
SUPABASE_CONTROL_URL=https://your-control-project.supabase.co
SUPABASE_CONTROL_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============ 워커 설정 (선택) ============

# 워커 식별자 (없으면 hostname 기반 자동 생성)
# EQUIPMENT_NAME=slot-naver-pc01

# Claude API (CAPTCHA 해결용)
# ANTHROPIC_API_KEY=sk-ant-여기에_API_KEY_입력

# IP 로테이션 (테더링 사용 시)
# IP_ROTATION_METHOD=auto
